/** Default USD threshold when `TELEGRAM_SPEND_ALERT_DEFAULT_USD` is unset and project has no override. */
export const DEFAULT_TELEGRAM_SPEND_ALERT_USD = 1;

/** Minimum estimated spend increase (USD) before sending another Telegram alert the same UTC day. */
export const DEFAULT_SPEND_ALERT_ESCALATION_MIN_DELTA_USD = 5;
